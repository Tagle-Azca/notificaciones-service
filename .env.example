# Servidor
PORT=3003
NODE_ENV=local

# AWS
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=tu_access_key
AWS_SECRET_ACCESS_KEY=tu_secret_key
AWS_SESSION_TOKEN=tu_session_token

# Nota: el email se envía vía suscripción Email directa en SNS.
# El lab no requiere SES. Suscribe tu email al topic SNS desde la consola.
